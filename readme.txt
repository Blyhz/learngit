Git is a version control system.
Git is a distributed version control system.
Git is free software distributed under the GPL.


If I change the txt file,then I can know the changes by the git command "git diff <filename>". However, once I "git add" the file, I could
not get anything from "git diff<filename>".

Make some change and see what will happen.